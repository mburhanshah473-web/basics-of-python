name="Burhan"
shname=name[1:5]
print(shname)
#here we used string index to slice the string
#we wrote the str indexes and there is a collon sign between them like in this case we wrote 1:5 so we start from the index 1 which corresponds to the letter "u" in the provided string and ends with index value 4 because index value written after the collon sign is not included 